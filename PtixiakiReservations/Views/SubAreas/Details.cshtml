@model PtixiakiReservations.Models.SubArea

@{
    ViewData["Title"] = "SubArea Details";
}

<div class="bg-gradient-to-r from-blue-50 to-indigo-50 min-h-screen py-8 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto">
        <!-- Header -->
        <div class="bg-white rounded-xl shadow-md overflow-hidden mb-8">
            <div class="bg-gradient-to-r from-blue-600 to-indigo-600 px-6 py-8">
                <div class="flex justify-between items-center">
                    <div>
                        <h1 class="text-2xl md:text-3xl font-bold text-white">Layout Details</h1>
                        <p class="mt-2 text-indigo-100">
                            Viewing details for layout: <span class="font-semibold">@Model.AreaName</span>
                        </p>
                    </div>
                    <div class="flex space-x-3">
                        <a href="@Url.Action("Edit", new { id = Model.Id })"
                           class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-indigo-700 bg-white hover:bg-indigo-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                            </svg>
                            Edit
                        </a>
                        @if (ViewBag.VenueId != null)
                        {
                            <a href="@Url.Action("VenueSubAreas", new { venueId = ViewBag.VenueId })"
                               class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-800 bg-opacity-70 hover:bg-opacity-90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                                </svg>
                                Back to @ViewBag.VenueName
                            </a>
                        }
                        else
                        {
                            <a href="@Url.Action("Index")"
                               class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-800 bg-opacity-70 hover:bg-opacity-90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                                </svg>
                                Back to List
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <div class="bg-white rounded-xl shadow-lg overflow-hidden">
            <!-- Information Sections -->
            <div class="p-6">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- Basic Information Card -->
                    <div class="bg-gray-50 rounded-lg p-5 border border-gray-200">
                        <div class="flex items-center mb-4">
                            <div class="h-10 w-10 rounded-md bg-indigo-100 flex items-center justify-center text-indigo-600">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                            </div>
                            <h2 class="ml-3 text-lg font-medium text-gray-800">Basic Information</h2>
                        </div>

                        <div class="space-y-4">
                            <div class="flex border-b border-gray-200 pb-3">
                                <dt class="w-1/3 font-medium text-gray-500">Area Name</dt>
                                <dd class="w-2/3 text-gray-900">@Model.AreaName</dd>
                            </div>
                            <div class="flex border-b border-gray-200 pb-3">
                                <dt class="w-1/3 font-medium text-gray-500">Venue</dt>
                                <dd class="w-2/3 text-gray-900">
                                    @if (Model.Venue != null)
                                    {
                                        @(Model.Venue.Name ?? $"ID: {Model.Venue.Id}")
                                    }
                                    else
                                    {
                                        <span class="text-gray-400">Not assigned</span>
                                    }
                                </dd>
                            </div>
                            @if (!string.IsNullOrEmpty(Model.Desc))
                            {
                                <div class="flex border-b border-gray-200 pb-3">
                                    <dt class="w-1/3 font-medium text-gray-500">Description</dt>
                                    <dd class="w-2/3 text-gray-900">@Model.Desc</dd>
                                </div>
                            }
                        </div>
                    </div>

                    <!-- Dimensions Card -->
                    <div class="bg-gray-50 rounded-lg p-5 border border-gray-200">
                        <div class="flex items-center mb-4">
                            <div class="h-10 w-10 rounded-md bg-indigo-100 flex items-center justify-center text-indigo-600">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 8V4m0 0h4M4 4l5 5m11-1V4m0 0h-4m4 0l-5 5M4 16v4m0 0h4m-4 0l5-5m11 5v-4m0 4h-4m4 0l-5-5" />
                                </svg>
                            </div>
                            <h2 class="ml-3 text-lg font-medium text-gray-800">Dimensions & Position</h2>
                        </div>

                        <div class="grid grid-cols-2 gap-4">
                            <div class="bg-white rounded-lg p-4 shadow-sm border border-gray-100">
                                <div class="text-sm font-medium text-gray-500 mb-1">Width</div>
                                <div class="text-xl font-bold text-indigo-600">@Model.Width</div>
                            </div>
                            <div class="bg-white rounded-lg p-4 shadow-sm border border-gray-100">
                                <div class="text-sm font-medium text-gray-500 mb-1">Height</div>
                                <div class="text-xl font-bold text-indigo-600">@Model.Height</div>
                            </div>
                            <div class="bg-white rounded-lg p-4 shadow-sm border border-gray-100">
                                <div class="text-sm font-medium text-gray-500 mb-1">Top Position</div>
                                <div class="text-xl font-bold text-indigo-600">@Model.Top</div>
                            </div>
                            <div class="bg-white rounded-lg p-4 shadow-sm border border-gray-100">
                                <div class="text-sm font-medium text-gray-500 mb-1">Left Position</div>
                                <div class="text-xl font-bold text-indigo-600">@Model.Left</div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Visual Representation (Placeholder) -->
                @* <div class="mt-8 bg-gray-50 rounded-lg p-5 border border-gray-200"> *@
                @*     <div class="flex items-center mb-4"> *@
                @*         <div class="h-10 w-10 rounded-md bg-indigo-100 flex items-center justify-center text-indigo-600"> *@
                @*             <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"> *@
                @*                 <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" /> *@
                @*             </svg> *@
                @*         </div> *@
                @*         <h2 class="ml-3 text-lg font-medium text-gray-800">Visual Representation</h2> *@
                @*     </div> *@
                @* *@
                @*     <div class="flex justify-center"> *@
                @*         <div class="relative border-2 border-dashed border-gray-300 rounded-lg p-4 bg-white" style="width: 400px; height: 300px;"> *@
                @*             <div class="absolute bg-indigo-100 border border-indigo-300 rounded text-center flex items-center justify-center" *@
                @*                  style="width: @(Model.Width > 0 ? Math.Min(Model.Width * 4, 380) : 100)px; *@
                @*                  height: @(Model.Height > 0 ? Math.Min(Model.Height * 4, 280) : 75)px; *@
                @*                  top: @(Model.Top > 0 ? Math.Min(Model.Top * 4, 280) : 10)px; *@
                @*                  left: @(Model.Left > 0 ? Math.Min(Model.Left * 4, 380) : 10)px;"> *@
                @*                 <div class="text-indigo-600 font-medium text-sm">@Model.AreaName</div> *@
                @*             </div> *@
                @*         </div> *@
                @*     </div> *@
                @*     <div class="text-center text-sm text-gray-500 mt-2"> *@
                @*         This is a simplified visual representation and may not be to scale. *@
                @*     </div> *@
                @* </div> *@

                <!-- Action Buttons at Bottom -->
                <div class="mt-8 flex justify-between items-center border-t border-gray-200 pt-6">
                    <div>
                        <a href="@Url.Action("Delete", new { id = Model.Id })"
                           class="inline-flex items-center px-4 py-2 border border-red-300 rounded-md text-sm font-medium text-red-700 bg-white hover:bg-red-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-colors">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                            </svg>
                            Delete
                        </a>
                    </div>
                    <div class="flex space-x-3">
                        @if (ViewBag.VenueId != null)
                        {
                            <a href="@Url.Action("VenueSubAreas", new { venueId = ViewBag.VenueId })"
                               class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h16M4 18h16" />
                                </svg>
                                Back to @ViewBag.VenueName
                            </a>
                        }
                        else
                        {
                            <a href="@Url.Action("Index")"
                               class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h16M4 18h16" />
                                </svg>
                                Back to List
                            </a>
                        }
                        <a href="@Url.Action("Edit", new { id = Model.Id })"
                           class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                            </svg>
                            Edit
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>